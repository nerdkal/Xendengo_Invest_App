git clone https://github.com/nerdkal/Xendengo_Invest_App
cd Xendengo_Invest_App/
git checkout App
git pull origin App

1. Database
sudo apt-get install gnupg curl -y
curl -fsSL https://www.mongodb.org/static/pgp/server-8.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-8.0.gpg --dearmor
echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg ] https://repo.mongodb.org/apt/ubuntu noble/mongodb-org/8.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
sudo apt-get update
sudo apt-get install -y mongodb-org

2. Iniciar o MongoDB e verificar status
sudo systemctl start mongod
sudo systemctl enable mongod  # inicia automaticamente no boot

3. Criar banco, coleção e usuário com permissões
echo -e "use ticker\ndb.createCollection('acoes')\ndb.createUser({    user: 'user',    pwd: 'pass',    roles: [        { role: 'readWrite', db: 'ticker' }     ] })" | mongosh

4. Instalar dependências do projeto
sudo npm install express axios cheerio mongoose mongodb dotenv -y

** Renomear .env.example para .env
** Rename .env.example to .env

 Rodar a aplicação
node src/index.js 

http://localhost:3000
